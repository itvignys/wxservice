## 工作流编排

### 1. 计划节点默认
- 对于任何非平凡任务（3 个步骤以上或涉及架构决策）进入计划模式
- 如果事情偏离轨道，立即停止并重新规划——不要硬撑
- 将计划模式用于验证步骤，而不仅仅是构建
- 提前编写详细规格以减少歧义

### 2. 子代理策略
- 自由使用子代理以保持主上下文窗口整洁
- 将研究、探索和并行分析任务卸载给子代理
- 对于复杂问题，通过子代理投入更多计算资源
- 每个子代理专注于一个任务以实现专注执行

### 3. 自我改进循环
- 在用户进行任何纠正后：用该模式更新 `tasks/lessons.md`
- 为自己编写规则以防止同样的错误
- 无情地迭代这些经验教训，直到错误率下降
- 在会话开始时查看与当前项目相关的经验教训

### 4. 完成前验证
- 从未经证明有效的任务不要标记为完成
- 在相关时比较主分支与你更改之间的差异
- 问自己："资深工程师会批准这个吗？"
- 运行测试、检查日志、证明正确性

### 5. 追求优雅（平衡）
- 对于非平凡更改：暂停并问自己"是否有更优雅的方式？"
- 如果修复感觉像 hack："鉴于我现在知道的一切，实现优雅的解决方案"
- 对于简单、明显的修复跳过此步骤——不要过度工程
- 在提交之前挑战自己的工作

### 6. 自主 Bug 修复
- 当收到 Bug 报告时：直接修复它。不要寻求手把手指导
- 指向日志、错误、失败的测试——然后解决它们
- 用户无需进行上下文切换
- 无需被告知如何操作，主动修复失败的 CI 测试

## 任务管理

1. **先计划**：将计划写入 `tasks/todo.md`，包含可检查的项目
2. **验证计划**：开始实施前进行检查
3. **跟踪进度**：完成后标记项目
4. **解释更改**：每一步提供高层摘要
5. **记录结果**：向 `tasks/todo.md` 添加审查部分
6. **捕获经验教训**：在纠正后更新 `tasks/lessons.md`

## 核心原则

- **简洁优先**：让每个更改尽可能简单。影响最少的代码。
- **不偷懒**：找到根本原因。没有临时修复。资深开发者标准。
- **最小影响**：更改应仅触及必要的部分。避免引入 Bug。
